// Namespaces require extra work in the generator, so they are not supported right now.
// namespace DevDriver.GpuClocks;

table ClockModeId
{
    id: uint32;
}

table ClockMode
{
    id: ClockModeId;
    name: string;
    description: string;
    gpuClock: uint64;
}

table ClockModeList
{
    modes: [ClockMode];
}
